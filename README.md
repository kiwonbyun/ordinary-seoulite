# Ordinary Seoulite

Vite + React SPA deployed to Cloudflare Pages, using Supabase for auth and data.

## Scripts

- `pnpm dev`
- `pnpm build`
- `pnpm test:run`

## Setup

Create `.env` from `.env.example` and configure required values for your local environment.
